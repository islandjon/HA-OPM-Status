#!/bin/bash

# Install dependencies
pip install -r requirements.txt

# Run the codebase
# Assuming this is a Home Assistant custom component, it should be run within Home Assistant.
# Start Home Assistant (assuming it's installed in a virtual environment in the current directory)
source venv/bin/activate
hass
